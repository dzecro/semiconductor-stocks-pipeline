# Python
venv/
__pycache__/
*.pyc
.env

# Database — excluded because it's generated by the pipeline
# Anyone cloning the repo runs load.py to generate their own
data/stocks.db

# VS Code — optional, remove if you want to share VS Code settings
.vscode/